<Window x:Class="Ezmacro.AddActionWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Ezmacro"
        mc:Ignorable="d"
        xmlns:sys="clr-namespace:System;assembly=mscorlib"
        Title="Add New Action" Height="280" Width="350"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="100"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Action Type -->
        <TextBlock Grid.Row="0" Grid.Column="0" Text="Action Type:" VerticalAlignment="Center" Margin="0,0,0,10"/>
        <ComboBox x:Name="CmbActionType" Grid.Row="0" Grid.Column="1" Margin="0,0,0,10"
                  SelectionChanged="CmbActionType_SelectionChanged">
            <sys:String>KeyPress</sys:String>
            <sys:String>KeyRelease</sys:String>
            <sys:String>MousePress</sys:String>
            <sys:String>MouseRelease</sys:String>
            <sys:String>MouseMove</sys:String>
        </ComboBox>

        <!-- Detail / Key -->
        <TextBlock x:Name="LblDetail" Grid.Row="1" Grid.Column="0" Text="Key / Button:" VerticalAlignment="Center" Margin="0,0,0,10"/>
        <ComboBox x:Name="CmbDetail" Grid.Row="1" Grid.Column="1" Margin="0,0,0,10" IsEditable="True"/>

        <!-- X Position -->
        <TextBlock Grid.Row="2" Grid.Column="0" Text="X Position:" VerticalAlignment="Center" Margin="0,0,0,10"/>
        <TextBox x:Name="TxtX" Grid.Row="2" Grid.Column="1" Text="0" Margin="0,0,0,10"/>

        <!-- Y Position -->
        <TextBlock Grid.Row="3" Grid.Column="0" Text="Y Position:" VerticalAlignment="Center" Margin="0,0,0,10"/>
        <TextBox x:Name="TxtY" Grid.Row="3" Grid.Column="1" Text="0" Margin="0,0,0,10"/>

        <!-- Delay -->
        <TextBlock Grid.Row="4" Grid.Column="0" Text="Delay (ms):" VerticalAlignment="Center" Margin="0,0,0,10"/>
        <TextBox x:Name="TxtDelay" Grid.Row="4" Grid.Column="1" Text="100" Margin="0,0,0,10"/>

        <!-- Buttons -->
        <StackPanel Grid.Row="6" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Add" Width="80" Height="26" Margin="0,0,10,0" IsDefault="True" Click="BtnAdd_Click"/>
            <Button Content="Cancel" Width="80" Height="26" IsCancel="True" Click="BtnCancel_Click"/>
        </StackPanel>
        
    </Grid>
</Window>
